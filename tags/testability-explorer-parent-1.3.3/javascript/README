#
# How to build!!
#

The version of Rhino that adds the Abstract Syntax tree isn't released yet. The pom lists a
dependency on rhino:js:1.7R3pre - to satisfy the dependency, you should run:

mvn install:install-file -Dfile=rhino_js_1.7R3pre.jar -DgroupId=rhino -DartifactId=js \
-Dversion=1.7R3pre -Dpackaging=jar -DcreateChecksum=true

This README (and the rhino_js_1.7R3pre.jar) will self-destruct when the next release happens.
