This directory contains jar files in a consistent and expected state. If the 
jars in <project root>/lib are ever upgraded, these will stay the same. 

Unit tests look at these to compute testability scores.