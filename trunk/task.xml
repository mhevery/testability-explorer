<project name="testability-explorer" default="build">

    <import file="base.xml"/>

    <target name="compile" depends="init">
        <javac srcdir="src" destdir="target/bin" debug="true" target="1.5" fork="true">
            <classpath refid="compile.classpath" />
            <include name="com/google/ant/**/*"/>
        </javac>
        <copy todir="target/bin">
            <fileset dir="src">
                <exclude name="**/*.java"/>
            </fileset>
        </copy>
    </target>

    <target name="compile-tests" depends="init">
        <javac srcdir="src-test" destdir="target/bin-test" debug="true" target="1.5" fork="trues">
            <classpath refid="test.classpath" />
            <include name="com/google/ant/**/*"/>
        </javac>
        <copy todir="target/bin">
            <fileset dir="src">
                <exclude name="**/*.java"/>
            </fileset>
        </copy>
    </target>

    <target name="test" depends="compile-tests">
        <junit printsummary="yes" haltonfailure="yes" fork="yes" maxmemory="750M">
            <classpath refid="test.classpath" />

            <formatter type="xml" usefile="true" />

            <batchtest fork="yes" todir="target/reports/junit">
                <fileset dir="src-test">
                    <include name="com/google/ant/*Test.java"/>
                    <include name="com/google/ant/**/*Test.java"/>
                </fileset>
            </batchtest>
        </junit>
    </target>

    <target name="jar" depends="revision">
        <taskdef name="jarjar" classname="com.tonicsystems.jarjar.JarJarTask" classpath="lib/jarjar.jar" />
        <jarjar jarfile="target/dist/ant-${project-name}-${version}-r${revision}.jar">
            <manifest>
                <attribute name="Version" value="${version}" />
                <attribute name="Revision" value="${revision}" />
            </manifest>
            <fileset dir="target/bin" includes="com/google/ant/**/*.*"/>
        </jarjar>
    </target>

    <target name="copy" depends="revision,compile,jar">
        <copy file="target/dist/ant-${project-name}-${version}-r${revision}.jar" tofile="lib/ant-${project-name}.jar"/>
        <copy file="target/dist/${project-name}-${version}-r${revision}.jar" tofile="lib/${project-name}.jar"/>
    </target>

    <target name="ant.task" depends="copy"/>

    <target name="main">
        <ant antfile="build.xml" target="jar"/>
    </target>
    <target name="build" depends="clean,main,init,compile,test,copy"/>
    <target name="build.task" depends="compile,test,copy"/>

</project>
